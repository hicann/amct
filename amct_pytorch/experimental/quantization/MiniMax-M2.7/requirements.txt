# 推荐优先使用容器环境：quay.io/ascend/vllm-ascend:v0.18.0rc1-a3
# 下列为 stage1 校准搜索 + stage2 融合导出 + stage3 MXFP4 转换所需的 Python 依赖。
# torch / torch_npu 建议直接使用容器内自带版本，无需通过本文件安装。
transformers==4.57.6
safetensors==0.7.0
accelerate==1.13.0
tqdm==4.67.3
tokenizers==0.22.2
numpy==2.1.3
packaging==25.0
# stage3 导出的 config.json 采用 quark 风格 quantization_config；MXFP4 权重打包本身由
# 纯 torch 的 mxfp4_quantizer 完成，不 import quark，但保留该依赖以对齐 quark 生态。
amd-quark==0.11.2
