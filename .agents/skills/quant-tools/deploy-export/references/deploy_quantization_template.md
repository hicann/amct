# Deploy Quantization Specification

## 1. 基本信息
- 模型名：
- 导出时间：
- 导出类型：直转 / PTQ / 混合
- 导出目录：
- 量化目标：
- 量化数据类型：
- 量化 bit 配置：

## 2. 下游最小交付物检查
> 这一节用于让通用下游消费者快速判断产物是否可直接接入。

| 交付物 | 是否齐备 | 路径 / 位置 | 备注 |
| --- | --- | --- | --- |
| `config.json` |  |  |  |
| `model.safetensors.index.json` |  |  |  |
| 量化权重 `*.safetensors` |  |  |  |
| `deploy_quantization.md` |  |  |  |

### 2.1 `config.json` 中的量化配置
- `quant_method`：
- `format`：
- `config_groups`：
- `ignore`：
- `kv_cache_scheme`：
- 下游是否可直接复用：
- 若不能直接复用，原因：

## 3. 量化位置总览
| 模块类型 | 是否量化 | 量化范围 | 算法类别 | 是否有额外计算 | 备注 |
| --- | --- | --- | --- | --- | --- |
| Attention Linear |  |  |  |  |  |
| Attention Cache |  |  |  |  |  |
| MLP |  |  |  |  |  |
| MoE Routed Experts |  |  |  |  |  |
| MoE Shared Expert |  |  |  |  |  |

## 4. 模块目标映射表
> 不要只写实验侧里的量化目标名，要写到下游运行对象这一层。

| 量化目标 | 实际层 / 前缀 | 下游运行对象 | 是否直接支持 | 不直接支持时的处理 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

建议至少说明：
- 哪些 target 映射到 `Linear`
- 哪些 target 映射到 `MoEGMM`
- 哪些 target 需要 KVCache 量化路径
- 哪些 target 暂不支持，只能回退

## 5. 导出张量清单
> 这一节要求写到下游可以直接按字段接入，不要只写张量名。

| 模块/位置 | 张量名 | dtype | shape | 语义 | 是否必需 | 使用阶段 | 读取方式 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |  |  |

建议至少覆盖：
- 量化权重本体，例如 `qweight`
- 权重量化系数，例如 `weight_scale`、`zero_point`
- 结构化算法附加参数，例如变换矩阵、旋转参数、额外 scale
- cache 量化附加参数

## 6. 量化策略明细
| 位置 | 数据类型 | 配置 | 量化算法 | 算法类别 | 是否有额外计算 | 导出附加参数 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |  |

算法类别建议固定为：
- 无额外计算代价
- 有额外计算代价
- Cache 量化

## 7. 模块级运行时契约
> 这一节是下游最需要的内容。要明确“输入是什么、读取哪些系数、按什么顺序执行、输出是什么”。

| 模块/位置 | 输入 | 读取张量 | 执行顺序 | 输出 | 约束条件 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |

执行顺序建议写成严格步骤，例如：
1. 读取 `qweight` 和 `weight_scale`
2. 若存在结构化参数，先对输入执行预处理变换
3. 执行量化线性计算或恢复计算
4. 若存在后处理，再执行对应变换
5. 输出到下一模块

## 8. 量化算法说明

### 8.1 无额外计算代价的量化
- 适用位置：
- 算法原理：
- 数学定义：
- 导出系数：
- 推理侧使用方式：
- 边界行为：
- 注意事项：

### 8.2 有额外计算代价的量化
- 适用位置：
- 算法原理：
- 数学定义：
- 运行时新增计算：
- 导出系数：
- 推理侧使用方式：
- 边界行为：
- 注意事项：

这一类至少说明：
- 新增计算发生在输入前、线性中还是输出后
- 变换是否可逆
- 量化权重与附加参数如何共同使用

### 8.3 Cache 量化
- 当前状态：
- 量化对象：
- 量化粒度：
- 非整除场景处理：
- 导出系数：
- 推理侧使用方式：
- 边界行为：
- 注意事项：

## 9. 数学与实现约束
> 这里不贴当前代码仓源码，但要给出下游能够实现的严格描述。

至少说明：
- 分组粒度 `group_size`
- 量化轴 `quant_axis`
- scale / zero_point 的广播规则
- 结构化参数的作用维度
- 是否要求输入长度、hidden size、cache 长度满足特定对齐
- decode 阶段与 prefill 阶段是否存在差异
- 是否需要额外的 `process_weights_after_loading()` 信息

## 10. 推理侧接入说明
- 需要读取的附加参数：
- 推理执行顺序：
- 是否依赖额外 kernel / 算子：
- 是否要求 group 对齐：
- 是否存在模块级差异：
- 当前下游可直接复用的能力：
- 当前下游仍需补实现的能力：

## 11. 回退策略与不支持场景
> 这一节必须显式写，不能靠下游猜。

- 可安全回退到浮点的模块：
- 必须停止接入的场景：
- 触发回退时需要保留哪些提示信息：
- 若是混合导出，哪些 target 实际仍为浮点：

## 12. 导出文件说明
| 文件名 | 作用 |
| --- | --- |
| `config.json` | 模型配置与量化配置 |
| model.safetensors.index.json | 权重索引 |
| layer_*.safetensors / rest_*.safetensors | 导出后的权重文件 |
| deploy_quantization.md | 主说明文档 |

## 13. 部署约束与风险提示
- 当前限制：
- 需要额外核对的点：
- 对下游的建议：
